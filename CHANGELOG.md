## Version 1.1.0

Add support for `prek` (rust implementation, faster, more user-friendly),
which is preferred by default, and allow overriding executable via config.

## Version 1.0.1

Use relative paths, fixing some configuration detection, e.g. `.prettierignore`

## Version 1.0

Initial release
